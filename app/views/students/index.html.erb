<%= form_tag students_path, method: 'get' do %>
  <%= text_field_tag 'student[name]' %>
  <%= submit_tag %>
<% end %>

<% @students.each do |one_student| %>
  <%= render partial: 'student', locals: {student: one_student} %>
<% end %>
